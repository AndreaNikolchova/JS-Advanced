const {assert} = require('chai');
const {rgbToHexColor} = require('../RGB to Hex.js');

describe('rgbToHexColor function',()=>{
    describe('red',()=>{
        it('red is not an integer')
    });
    
});